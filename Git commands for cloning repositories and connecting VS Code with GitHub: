1. Set Up Git (If Not Already Installed):
git config --global user.name "Your Name"
git config --global user.email "youremail@example.com"

2. Clone a GitHub Repository:
git clone https://github.com/username/repository-name.git

3. Open the Repository in VS Code:
cd repository-name
code .

4. Initialize Git in a New Project (if you're starting from scratch):
git init
-then add files:
git add .
git commit -m "Initial commit"

 5. Connect to GitHub Repository:
git remote add origin https://github.com/username/repository-name.git
git branch -M main
git push -u origin main

 6. Push Changes to GitHub:
git add .
git commit -m "Your commit message"
git push

 7. Check Git Status:
git status

8. Pull Latest Changes from GitHub:
git pull


---These Git commands help with branching, syncing, fixing mistakes, and collaborating:
🔁 BRANCHING (Working on Features):
➤ Create a New Branch
bash
Copy code
git checkout -b feature-branch-name
➤ Switch to a Branch
bash
Copy code
git checkout main
➤ See All Branches
bash
Copy code
git branch
➤ Delete a Branch (locally)
bash
Copy code
git branch -d feature-branch-name

🔄 SYNCING CHANGES
➤ Fetch Remote Changes (metadata only)
bash
Copy code
git fetch
➤ Pull and Merge Latest Changes
bash
Copy code
git pull origin main
➤ Push a New Branch to GitHub
bash
Copy code
git push -u origin feature-branch-name

✅ STAGING AND COMMITTING
➤ Stage Specific File
bash
Copy code
git add filename.py
➤ Unstage a File (if added by mistake)
bash
Copy code
git reset HEAD filename.py
➤ Undo Last Commit (keep changes)
bash
Copy code
git reset --soft HEAD~1
➤ See Commit History
bash
Copy code
git log
Use q to quit the log view.

🧹 CLEANUP / FIXING
➤ Discard Local File Changes (not committed)
bash
Copy code
git checkout -- filename
➤ Remove a Tracked File from Git
bash
Copy code
git rm filename
git commit -m "Remove file"
➤ See Changes in Files
bash
Copy code
git diff

 REMOTE MANAGEMENT
➤ Check Remote URLs
bash
Copy code
git remote -v
➤ Change Remote URL (e.g., if you rename repo)
bash
Copy code
git remote set-url origin https://github.com/username/new-repo.git



